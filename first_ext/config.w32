// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("first_ext", "for first_ext support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("first_ext", "enable first_ext support", "no");

if (PHP_FIRST_EXT != "no") {
	EXTENSION("first_ext", "first_ext.c");
}

